Next time...
-make it so current transforms append transformed images to original dict
-implement normalized contrast per image transformation

Some day...
-Add per image contrast adjustment as a transform to Monai? Take array of normalized intensities as input?